# {{ user_name }} 私の Qiita ポートフォリオ

※この記事は自動生成です。

[hassaku_63](https://qiita.com/hassaku_63) が投稿した記事を集計し、サマリを表示しています

## Pickups

Articles sorted by likes

{# Like 数を表示 #}
{# {% if items is not none %} #}
{# {% for item in items %} #}
{# - [{{item.name}}]({{ item.link }}) / likes = {{ item.likes }} #}
{# {% endfor %} #}
{# {% else %} #}
{#     No available items. #}
{# {% endif %} #}

{%- if items is not none %}
|Likes|Article|
|:---:|:---|
{%- for item in items %}
|{{ item.likes }} likes|[{{item.name}}]({{ item.link }})|
{%- endfor %}

{%- else %}
No available items.
{%- endif %}

## Frequent tags

{# タグ分布を表示 #}
{# {% if tags_count is not none %} #}
{# {% for tag in tags_count %} #}
{# - {{ tag.name }} {{ tag.count }} #}
{# {% endfor %} #}
{# {% else %} #}
{#     No available items. #}
{# {% endif %} #}

{%- if tags_count is not none %}
|Count|Tag|
|:---:|:---:|
{%- for tag in tags_count %}
|{{ tag.name }}|{{ tag.count }}|
{%- endfor %}

{%- else %}
No available items.
{%- endif %}


# Appendix: About this article

This article is generated by;

- Serverless Framework
- AWS
    - Step Functions
    - Lambda
    - SQS
    - DynamoDB
    - etc

This article will updates everyday, probably...

I'm going to share with you an article that explains the source code and the architecture of this auto-generation system .... sometime.